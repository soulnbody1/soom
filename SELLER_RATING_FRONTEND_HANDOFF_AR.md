# تسليم ميزة تقييم البائع للفرونت

## الفكرة

التقييم يخص البائع نفسه، وليس إعلانًا أو منتجًا معينًا. يصل الفرونت إلى البائع عادةً من `user.id` الموجود داخل بيانات الإعلان، ثم يستخدم هذا المعرّف لفتح صفحة البائع والتعامل مع تقييمه.

كل مستخدم يستطيع امتلاك تقييم واحد فقط لكل بائع. أول حفظ ينشئ التقييم، وأي حفظ لاحق يعدّل التقييم نفسه. يستطيع المستخدم حذف تقييمه، ولا يستطيع تقييم نفسه أو تعديل تقييم مستخدم آخر.

## التصور الوظيفي للصفحة

هذه ليست مواصفة تصميم ملزمة. المطلوب أن توصل الصفحة المعاني التالية، وللفرونت الحرية الكاملة في شكل العرض والتفاعل:

- هوية البائع العامة: الاسم والصورة والموقع وتاريخ الانضمام وعدد الإعلانات.
- ملخص الثقة: متوسط النجوم، إجمالي عدد التقييمات، وتوزيع التقييمات من 5 إلى 1.
- حالة المستخدم الحالي: هل يمكنه التقييم، وهل لديه تقييم سابق يمكن تعديله أو حذفه.
- نموذج بسيط لاختيار درجة من 1 إلى 5 وكتابة رسالة.
- قائمة رسائل التقييم من الأحدث إلى الأقدم مع pagination.
- حالات واضحة للتحميل، وعدم وجود تقييمات، وفشل الحفظ، والحفظ الناجح.

يمكن تحميل بيانات صفحة البائع أولًا، ثم تحميل قائمة التقييمات بالتوازي أو عند الوصول إلى قسم التقييمات. بعد الحفظ أو الحذف يفضّل إعادة تحميل ملخص الصفحة والقائمة حتى يظل المتوسط والعدد والتوزيع متطابقين مع الخادم.

## عرض صفحة البائع

`GET /api/soom/sellers/{seller}`

المسار عام. عند وجود Bearer token صالح يعيد الخادم أيضًا تقييم المستخدم الحالي وحالة قدرته على التقييم.

```json
{
  "success": true,
  "message": "تم جلب صفحة البائع بنجاح.",
  "data": {
    "id": 25,
    "name": "اسم البائع",
    "logo": "https://example.com/seller.jpg",
    "location": "الأردن، عمان، صويلح",
    "ads_count": 8,
    "joined_at": "2026-09-13T12:00:00+03:00",
    "rating_summary": {
      "average": 4.5,
      "count": 12,
      "distribution": [
        { "rating": 5, "count": 8 },
        { "rating": 4, "count": 2 },
        { "rating": 3, "count": 1 },
        { "rating": 2, "count": 1 },
        { "rating": 1, "count": 0 }
      ]
    },
    "viewer": {
      "can_rate": true,
      "rating": {
        "id": 90,
        "rating": 5,
        "message": "بائع محترم وسريع في التعامل.",
        "reviewer": {
          "id": 11,
          "name": "اسم المستخدم",
          "logo": null
        },
        "created_at": "2026-09-13T12:30:00+03:00",
        "updated_at": "2026-09-13T12:30:00+03:00"
      }
    }
  }
}
```

إذا لم يكن الزائر مسجلًا يكون `viewer.can_rate` بقيمة `false` و`viewer.rating` بقيمة `null`. إذا كان مسجلًا وليس لديه تقييم سابق يكون `can_rate` بقيمة `true` و`rating` بقيمة `null`. إذا كان المستخدم هو البائع نفسه يكون `can_rate` بقيمة `false`.

## عرض رسائل التقييم

`GET /api/soom/sellers/{seller}/ratings?page=1&per_page=15`

المسار عام، والترتيب من الأحدث إلى الأقدم. القيمة الافتراضية لـ `per_page` هي 15 والحد الأقصى 50.

```json
{
  "data": [
    {
      "id": 90,
      "rating": 5,
      "message": "بائع محترم وسريع في التعامل.",
      "reviewer": {
        "id": 11,
        "name": "اسم المستخدم",
        "logo": null
      },
      "created_at": "2026-09-13T12:30:00+03:00",
      "updated_at": "2026-09-13T12:30:00+03:00"
    }
  ],
  "links": {},
  "meta": {
    "current_page": 1,
    "last_page": 1,
    "per_page": 15,
    "total": 1
  },
  "success": true,
  "message": "تم جلب تقييمات البائع بنجاح."
}
```

`links` و`meta` هما حقول Laravel pagination المعتادة، وقد يحتوي `links` على روابط `first` و`last` و`prev` و`next`.

## إضافة أو تعديل التقييم

`PUT /api/soom/sellers/{seller}/rating`

يتطلب Bearer token لحساب مستخدم عادي.

```json
{
  "rating": 5,
  "message": "بائع محترم وسريع في التعامل"
}
```

- `rating` رقم صحيح مطلوب من 1 إلى 5.
- `message` نص مطلوب، يتم حذف المسافات الزائدة من بدايته ونهايته، والحد الأقصى 1000 حرف.
- الاستجابة `201` إذا كان هذا أول تقييم للمستخدم مع البائع.
- الاستجابة `200` إذا تم تعديل تقييم سابق.
- جسم الاستجابة يحتوي على التقييم المحفوظ داخل `data` بنفس شكل عنصر قائمة التقييمات.

الفرونت لا يحتاج مسارًا منفصلًا للتعديل، ولا يرسل `product_id` أو `ad_id` أو `reviewer_id`. هوية المقيّم تؤخذ من التوكن، وهوية البائع تؤخذ من المسار.

## حذف التقييم

`DELETE /api/soom/sellers/{seller}/rating`

يتطلب Bearer token، ويحذف تقييم المستخدم الحالي فقط.

```json
{
  "success": true,
  "message": "تم حذف تقييم البائع بنجاح.",
  "data": []
}
```

## الأخطاء المتوقعة

الأخطاء ترجع بالشكل الموحد للمشروع:

```json
{
  "success": false,
  "message": "The given data was invalid.",
  "code": "validation_failed",
  "errors": {
    "rating": ["درجة التقييم يجب أن تكون بين 1 و5."]
  }
}
```

- `401`: لا يوجد توكن صالح في مسار الحفظ أو الحذف.
- `403`: نوع الحساب غير مسموح له بالكتابة.
- `404`: البائع غير موجود، أو لا يوجد تقييم للمستخدم عند طلب الحذف.
- `422`: الدرجة أو الرسالة غير صحيحة، أو المستخدم يحاول تقييم نفسه.
- `429`: تم تجاوز معدل الطلبات المسموح.

## ملاحظات التكامل

- استخدم `user.id` الموجود في الإعلان باعتباره `{seller}`.
- لا تعتمد على متوسط محسوب في الفرونت؛ استخدم `rating_summary` من الخادم.
- اعرض المتوسط بصفر عند عدم وجود تقييمات، وسيكون التوزيع موجودًا دائمًا بكل الدرجات الخمس.
- بيانات المقيّم عامة ومحدودة في `id` و`name` و`logo`، ولا يعيد المسار هاتفه أو بريده.
- بعد نجاح الحفظ يمكن تحويل النموذج إلى وضع التعديل اعتمادًا على `viewer.rating`.
- عند `viewer.can_rate = false` يستطيع الفرونت إخفاء نموذج التقييم أو استبداله بالتصرف الأنسب لتجربة الاستخدام.
